<Window x:Class="X4ModLauncher.ThemedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="X4 Mod Launcher" Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent" Foreground="{StaticResource TextBrush}">
  <Border Background="{StaticResource PanelBrush2}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" Padding="1">
    <Grid PreviewMouseLeftButtonDown="WindowDrag">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="14,10">
        <Grid>
          <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" />
          <Button Grid.Column="1" Content="×" Width="30" Height="26" Padding="0" Margin="0" HorizontalAlignment="Right" Click="Cancel_Click" ToolTip="Close" />
        </Grid>
      </Border>

      <TextBlock Grid.Row="1" x:Name="MessageText" Margin="18,16,18,8" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" />

      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="18,8,18,16">
        <Button x:Name="ConfirmButton" Width="110" Click="Confirm_Click" HorizontalContentAlignment="Center" />
        <Button x:Name="CancelButton" Width="110" Click="Cancel_Click" HorizontalContentAlignment="Center" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
